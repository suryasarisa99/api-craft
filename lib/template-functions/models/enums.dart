enum ArgumentType {
  text,
  select,
  accordion,
  editor,
  checkbox,
  file,
  httpRequest,
  hStack,
  banner,
  markdown,
}

enum Purpose { send, preview }

enum Return { first, last, all, join }

enum Behavior { smart, always, ttl, never }
